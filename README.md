# React client for the Next.js Hello API

This React project connects to the Next.js backend and displays the `message` returned by `GET /api/hello`.

## Run locally

1. Start the Next.js backend on port 3000.
2. Run `npm install`.
3. Run `npm run dev`.
4. Open `http://localhost:5173/test_api`.
